if 'BUILD_CDC_ONLY'
  subdir('ob_error')
  subdir('ob_admin')
else
  subdir('ob_error')
  subdir('ob_admin')
endif
subdir('upgrade')
if 'OB_BUILD_CLOSE_MODULES'
  subdir('agentserver')
  subdir('ob_bench')
endif
