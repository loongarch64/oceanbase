Index: cos-c-sdk-v5-5.0.22/CMakeLists.txt
===================================================================
--- cos-c-sdk-v5-5.0.22.orig/CMakeLists.txt
+++ cos-c-sdk-v5-5.0.22/CMakeLists.txt
@@ -1,6 +1,6 @@
 PROJECT(cos_c_sdk)
 
-CMAKE_MINIMUM_REQUIRED(VERSION 2.6)  
+cmake_minimum_required(VERSION 4.0)
 
 set(CMAKE_VERSION 1.0.1)
 
@@ -79,7 +79,7 @@ ENDIF()
 
 #curl-config
 FIND_PROGRAM(CURL_CONFIG_BIN NAMES curl-config)
-  
+
 IF (CURL_CONFIG_BIN)
     EXECUTE_PROCESS(
         COMMAND ${CURL_CONFIG_BIN} --libs
@@ -89,7 +89,7 @@ IF (CURL_CONFIG_BIN)
 ELSE()
     MESSAGE(FATAL_ERROR "Could not find curl-config")
 ENDIF()
-  
+
 # Compile and link lib_cos_c_sdk
 include_directories(${APR_INCLUDE_DIR})
 include_directories(${APR_UTIL_INCLUDE_DIR})
