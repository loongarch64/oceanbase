cos_wrapper_lib = library('cos_wrapper', 'OBJECT', 'ob_singleton.h', 'ob_cos_wrapper.h', 'ob_cos_wrapper.cpp')
# target_include_directories(['cos_wrapper', 'PUBLIC', dep_dir, '/include/apr-1', dep_dir, '/include', dep_dir, '/include/cos_c_sdk', dep_dir, '/include/mxml'])
# target_link_libraries(['cos_wrapper', 'PUBLIC', 'oblib_base'])
# we need to get all .o files from cos-c-sdk rpm and mixed them into one .o file
